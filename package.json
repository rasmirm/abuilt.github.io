{
  "name": "netlify-function",
  "version": "1.0.0",
  "description": "Minimal Netlify function with sharp",
  "main": "classify.js",
  "scripts": {
    "start": "netlify dev"
  },
  "dependencies": {
    "formidable": "^2.0.1",
    "sharp": "^0.30.0"  // Ensure you use a compatible version
  }
}
